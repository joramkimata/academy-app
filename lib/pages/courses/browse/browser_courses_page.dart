import 'package:flutter/material.dart';

class BrowseCoursesPage extends StatelessWidget {
  const BrowseCoursesPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Browse Courses Page'),
    );
  }
}
